<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.mondial2030.controller.SignupModalController"
      alignment="CENTER" spacing="15" styleClass="modal-container">
    
    <padding>
        <Insets top="30" right="40" bottom="30" left="40"/>
    </padding>
    
    <!-- Header -->
    <VBox alignment="CENTER" spacing="10">
        <Label text="🏟️" style="-fx-font-size: 40px;"/>
        <Label text="Créer un Compte" styleClass="modal-title"/>
        <Label text="Rejoignez Mondial 2030" styleClass="modal-subtitle"/>
    </VBox>
    
    <!-- Signup Form -->
    <VBox spacing="12" maxWidth="300">
        
        <VBox spacing="3">
            <Label text="Nom complet"/>
            <TextField fx:id="nameField" promptText="Votre nom complet" styleClass="form-field"/>
        </VBox>
        
        <VBox spacing="3">
            <Label text="Email"/>
            <TextField fx:id="emailField" promptText="votre@email.com" styleClass="form-field"/>
        </VBox>
        
        <VBox spacing="3">
            <Label text="Nom d'utilisateur"/>
            <TextField fx:id="usernameField" promptText="Choisissez un nom d'utilisateur" styleClass="form-field"/>
        </VBox>
        
        <VBox spacing="3">
            <Label text="Mot de passe"/>
            <PasswordField fx:id="passwordField" promptText="Minimum 6 caractères" styleClass="form-field"/>
        </VBox>
        
        <VBox spacing="3">
            <Label text="Confirmer le mot de passe"/>
            <PasswordField fx:id="confirmPasswordField" promptText="Répétez le mot de passe" styleClass="form-field"/>
        </VBox>
        
        <Label fx:id="errorLabel" styleClass="error-label" visible="false" wrapText="true"/>
        <Label fx:id="successLabel" styleClass="success-label" visible="false" wrapText="true"/>
        
        <Button fx:id="signupButton" text="Créer mon compte" onAction="#handleSignup" 
                styleClass="primary-button" maxWidth="Infinity"/>
        
        <Separator/>
        
        <HBox alignment="CENTER" spacing="5">
            <Label text="Déjà un compte?"/>
            <Hyperlink text="Se connecter" onAction="#handleSwitchToLogin"/>
        </HBox>
        
    </VBox>
    
    <!-- Close button -->
    <Button text="Fermer" onAction="#handleClose" styleClass="secondary-button"/>
    
</VBox>
