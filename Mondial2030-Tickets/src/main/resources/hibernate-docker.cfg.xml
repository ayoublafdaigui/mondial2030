<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- PostgreSQL JDBC Configuration -->
        <!-- Note: For Docker, use environment variables or update these values -->
        <property name="connection.driver_class">org.postgresql.Driver</property>
        <property name="connection.url">jdbc:postgresql://db:5432/mondial2030</property>
        <property name="connection.username">mondial_user</property>
        <property name="connection.password">mondial_pass</property>

        <!-- HikariCP Connection Pool Configuration -->
        <property name="hibernate.connection.provider_class">org.hibernate.hikaricp.internal.HikariCPConnectionProvider</property>
        <property name="hibernate.hikari.minimumIdle">5</property>
        <property name="hibernate.hikari.maximumPoolSize">20</property>
        <property name="hibernate.hikari.idleTimeout">30000</property>
        <property name="hibernate.hikari.connectionTimeout">20000</property>
        <property name="hibernate.hikari.maxLifetime">1800000</property>

        <!-- PostgreSQL Dialect -->
        <property name="dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Schema Management: create-drop for dev, update for prod -->
        <property name="hbm2ddl.auto">update</property>
        
        <!-- SQL Logging (set to false in production) -->
        <property name="show_sql">true</property>
        <property name="format_sql">true</property>
        <property name="use_sql_comments">true</property>

        <!-- Performance Settings -->
        <property name="hibernate.jdbc.batch_size">20</property>
        <property name="hibernate.order_inserts">true</property>
        <property name="hibernate.order_updates">true</property>
        
        <!-- Connection validation -->
        <property name="hibernate.connection.provider_disables_autocommit">false</property>

        <!-- Entity Mappings -->
        <mapping class="com.mondial2030.model.User"/>
        <mapping class="com.mondial2030.model.Stadium"/>
        <mapping class="com.mondial2030.model.Match"/>
        <mapping class="com.mondial2030.model.Ticket"/>
        <mapping class="com.mondial2030.model.TicketTransfer"/>
    </session-factory>
</hibernate-configuration>
